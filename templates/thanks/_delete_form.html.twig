<form method="post" action="{{ path('app_thanks_delete', {'id': thank.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ thank.id) }}">
    <button class="btn">Delete</button>
</form>
